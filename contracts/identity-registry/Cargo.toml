[package]
name = "identity-registry"
version = "0.1.0"
edition = "2021"
publish = false
rust-version = "1.84.0"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
